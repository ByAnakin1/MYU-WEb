---
// components/WelcomePopup.astro
---

<style>
    #overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        z-index: 1000;
        display: flex;
        justify-content: center;
        align-items: center;
        cursor: default;
    }

    #welcome-popup {
        background-color: #ffffff;
        padding: 30px;
        border-radius: 15px;
        box-shadow: 0 8px 16px rgba(0,0,0,0.2);
        max-width: 400px;
        text-align: center;
        position: relative;
        animation: popup-enter 0.5s ease-out;
    }

    #close-popup {
        position: absolute;
        top: 10px;
        right: 10px;
        cursor: pointer;
        font-size: 24px;
        color: #666;
        transition: color 0.3s ease;
    }

    #close-popup:hover {
        color: #000;
    }

    @keyframes popup-enter {
        from {
            opacity: 0;
            transform: scale(0.7);
        }
        to {
            opacity: 1;
            transform: scale(1);
        }
    }
</style>

<div id="overlay">
    <div id="welcome-popup">
        <span id="close-popup">×</span>
        <h2>¡Bienvenido a MYU WEB!</h2>
        <p>Gracias por visitarnos. Descubre nuestros productos especiales y únicas ofertas.</p>
        <button id="close-btn" style="
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #a08c8c;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        ">Entendido</button>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const overlay = document.getElementById('overlay');
        const closePopup = document.getElementById('close-popup');
        const closeBtn = document.getElementById('close-btn');

        // Función para cerrar el popup
        function hidePopup() {
            overlay.style.display = 'none';
            document.body.style.removeProperty('overflow');
        }

        // Bloquear scroll al cargar
        document.body.style.overflow = 'hidden';

        // Cerrar popup con la X
        closePopup.addEventListener('click', hidePopup);

        // Cerrar popup con el botón
        closeBtn.addEventListener('click', hidePopup);

        // Desaparecer después de 10 segundos
        setTimeout(function() {
            hidePopup();
        }, 10000);

        // Cerrar si se hace clic fuera del popup
        overlay.addEventListener('click', function(event) {
            if (event.target === overlay) {
                hidePopup();
            }
        });
    });
</script>