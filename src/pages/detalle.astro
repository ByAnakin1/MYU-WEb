---
import Footer from "../components/footer.astro";
import Header from "../components/header.astro";
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Myu Fashion Shop</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .custom-button {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 10px 20px;
            border-radius: 25px;
            font-weight: bold;
            width: 100%;
            margin-top: 10px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .custom-button:hover {
            transform: translate(5px, -5px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .cart-button {
            background-color: #FFD1E0;
            color: #4A3E4C;
        }
        .whatsapp-button {
            background-color: #FFD1E0;
            color: #4A3E4C;
        }
        .button-icon {
            margin-right: 8px;
        }
        .custom-list {
            list-style-type: disc;
            padding-left: 20px;
        }
        .custom-list li {
            margin-bottom: 8px;
        }
        .custom-list {
            color: #e8e7e1;
        }
        .custom-list li::marker {
            color: #e8e7e1;
        }
        #map {
            width: 100%;
            height: 300px;
        }
        .thumbnail-container {
            display: flex;
            gap: 8px;
            overflow-x: auto;
            padding: 8px 0;
        }
        .thumbnail {
            cursor: pointer;
            border-radius: 8px;
            transition: transform 0.2s ease;
        }
        .thumbnail:hover {
            transform: scale(1.05);
        }
        .image-gallery-container {
            position: relative;
        }
        .nav-button {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background: white;
            border: none;
            border-radius: 50%;
            padding: 8px;
            cursor: pointer;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }
        .nav-button:hover {
            background-color: #f0f0f0;
        }
        .nav-button svg {
            width: 24px;
            height: 24px;
        }
        #prevBtn {
            left: 8px;
        }
        #nextBtn {
            right: 8px;
        }
    </style>
</head>
<body class="bg-[#FFECF4] font-sans text-[#444444]">
    <Header />
    <div class="h-16"></div> <!-- Espacio para separar el header del contenido -->
    <!-- Product Section -->
    <section class="container mx-auto p-5 md:p-10 mt-10 lg:mt-20">
        <!-- Main Image and Navigation -->
        <div class="image-gallery-container relative">
            <!-- Main Image -->
            <img id="mainImage" src="img/falda37.jpg" alt="Falda Principal" class="w-full h-auto rounded-lg mb-4 lg:mb-0">
            
            <!-- Navigation Arrows -->
            <button id="prevBtn" class="nav-button lg:hidden">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
            </button>
            <button id="nextBtn" class="nav-button lg:hidden">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
            </button>
        </div>
        
        <!-- Thumbnail Images -->
        <div class="thumbnail-container mt-4">
            <img src="img/falda37.jpg" alt="Falda #28" class="thumbnail w-24 h-24 rounded-lg" onclick="changeImage('img/falda37.jpg')">
            <img src="img/falda37l.jpg" alt="Vista 1" class="thumbnail w-24 h-24 rounded-lg" onclick="changeImage('img/falda37l.jpg')">
            <div class="thumbnail bg-gray-200 h-24 w-24 rounded-lg"></div>
            <div class="thumbnail bg-gray-200 h-24 w-24 rounded-lg"></div>
        </div>

        <!-- Product Details -->
        <div class="bg-[#FFE6EE] p-5 md:p-8 rounded-lg shadow-lg mt-8">
            <h2 class="text-[#4A3E4C] text-2xl md:text-3xl font-bold">FALDA #28</h2>
            <div class="mt-4">
                <p class="font-semibold">MATERIAL:</p>
                <p class="text-gray-600">Algodón suave con un toque de elastano para mayor comodidad y ajuste.</p>
            </div>
            <div class="mt-4">
                <p class="font-semibold">DISEÑO:</p>
                <p class="text-gray-600">Clásico y elegante con un corte A-line que se ajusta a la cintura y fluye hacia abajo, proporcionando un look atemporal.</p>
            </div>
            <div class="mt-4">
                <span class="font-semibold">COLOR:</span>
                <span>Blanco</span>
            </div>
            <div class="mt-4">
                <span class="font-semibold">TALLA:</span>
                <div class="flex flex-wrap items-center mt-2 gap-4">
                    <label class="inline-flex items-center">
                        <input type="radio" class="form-radio text-pink-600" name="size" value="S">
                        <span class="ml-2">S</span>
                    </label>
                    <label class="inline-flex items-center">
                        <input type="radio" class="form-radio text-pink-600" name="size" value="M">
                        <span class="ml-2">M</span>
                    </label>
                    <label class="inline-flex items-center">
                        <input type="radio" class="form-radio text-pink-600" name="size" value="L">
                        <span class="ml-2">L</span>
                    </label>
                </div>
            </div>
            <div class="mt-4">
                <span class="font-semibold">SKU:</span>
                <span>0050</span>
            </div>
            <div class="mt-4 text-2xl md:text-3xl text-[#FF71A9] font-bold">S/. 45.00</div>
            <div class="mt-4 flex flex-col gap-4">
                <button class="custom-button cart-button">
                    <span class="button-icon"></span>
                    AGREGAR AL CARRITO <img src="img/carrito-kawai.png" class="w-12 h-12 md:w-13 md:h-11 ml-2 md:ml-4">
                </button>
                <button class="custom-button whatsapp-button">
                    <span class="button-icon"></span>
                    CONTINUAR COMPRA A <img src="img/wasa-kawai.png" class="w-12 h-12 md:w-13 md:h-11 ml-2 md:ml-4">
                </button>
            </div>
        </div>
    </section>
    <!-- Footer -->
    <Footer />

    <script type="module">
        const images = [
            'img/falda37.jpg',
            'img/falda37l.jpg',
            // Agrega más imágenes aquí según sea necesario
        ];

        let currentImageIndex = 0;

        function changeImage(src) {
            document.getElementById('mainImage').src = src;
            currentImageIndex = images.indexOf(src);
        }

        function prevImage() {
            currentImageIndex = (currentImageIndex - 1 + images.length) % images.length;
            changeImage(images[currentImageIndex]);
        }

        function nextImage() {
            currentImageIndex = (currentImageIndex + 1) % images.length;
            changeImage(images[currentImageIndex]);
        }

        document.getElementById('prevBtn').addEventListener('click', prevImage);
        document.getElementById('nextBtn').addEventListener('click', nextImage);
    </script>
</body>
</html>
